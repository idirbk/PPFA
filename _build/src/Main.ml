(*
   PPFA
   Boukari Idir 
   Berkennou Brahim
   Université Paris-Saclay
*)

open Tools
open Game
open Network


let fd = install_client ;;
(*
send_pseudo oc "idir";;

let liste_perso = invincible::predactor::equil1::[] in
send_perso oc liste_perso;;
let num = int_of_string (input_line ic) in
  Printf.printf " le numero == %d" num;

*)